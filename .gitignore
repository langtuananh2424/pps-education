# Java / Maven
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# Log output từ chạy app cục bộ (mvn spring-boot:run > *.log) để verify runtime
*.log

# File upload cục bộ (MediaStorageService, mvn spring-boot:run không qua Docker)
pps-education-backend/media-storage/

# Env / secrets
.env
.env.*
!.env.example
*.local.yml
application-local.yml
*.pem
*.key
*.p12
*.pfx

# Frontend (thêm khi bắt đầu Frontend Phase 1)
node_modules/
dist/
build/
.vite/
docker-compose.override.yml

# Project thử nghiệm local, không đẩy lên GitHub
pps-education-frontend-test/

# Rule cá nhân của user (máy dev quá nhiệt) — không áp dụng cho cả team
.claude/rules/local-ci-testing.md

# Playwright MCP — công cụ + output phát sinh khi QA test live qua UI thật
.mcp.json
.playwright-mcp/

# File thử nghiệm/scratch cục bộ (VD k6 loadtest, sample input/output khi tự tay
# verify report generation) — phát hiện nằm untracked lâu ngày khi audit trước
# lúc public repo (2026-08-27), thêm ignore để tránh lỡ tay `git add -A` commit nhầm.
pps-education-backend/loadtest/
pps-education-backend/test_sample_*.docx
pps-education-backend/test_sample_*.html
pps-education-backend/test_sample_*.pdf
